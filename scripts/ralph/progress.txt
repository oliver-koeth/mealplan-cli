# Ralph Progress Log
Started: Wed Apr  8 15:40:02 CEST 2026

## Codebase Patterns
- For canonical `YYYYMMDD` date fields in application boundary contracts, reuse one shared validator helper in `src/mealplan/application/contracts.py` so boundary parse failures remain deterministic (`date: Value error, expected YYYYMMDD`).
- For UUID-keyed JSON persistence adapters, mirror `JsonCalendarStore` conventions: auto-create missing files with `{}` root, raise deterministic `ConfigError` prefixes (`log.store:`), and keep not-found domain errors namespaced (`log.<uuid>: entry not found`).
- For food-log retrieval semantics, keep filtering centralized in `JsonFoodLogStore.search` with optional-AND behavior (`date` exact, `meal` exact, `name` case-insensitive substring) and return canonical `FoodLogEntry` rows sorted newest-first.
- For new CLI persistence features, follow the env-override pattern used by calendar/log commands: read `MEALPLAN_*_STORE_PATH` first and default to a stable `~/.mealplan/*.json` path so tests can isolate storage and CLI behavior stays deterministic.
- For web write endpoints that target UUID-keyed persistence, treat the route path UUID as canonical (`PUT /api/v1/log/{uuid}`), inject it into parsed contracts before store updates, and map `log.<uuid>: entry not found` to structured HTTP `404` API errors.

## [2026-04-08 15:43:02 CEST] - US-001
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-1.txt
- What was implemented
- Added canonical food-log contract DTOs in the application boundary: `FoodLogUpsertRequest` (optional `uuid`, default `quantity=1.0`), `FoodLogSearchRequest` (optional `date`/`name`/`meal` filters), and `FoodLogEntry` response shape (`uuid`, `date`, `meal`, `name`, `kcal`, `carbs`, `fat`, `protein`, `fiber`).
- Added shared canonical date normalization in contracts and wired `field_validator` checks on all food-log date fields to enforce strict `YYYYMMDD`.
- Extended unit and parsing tests to cover food-log DTO parsing, default quantity behavior, optional filter behavior, canonical response serialization, and deterministic parse error mapping for invalid dates.
- Ran quality checks successfully using `uv run ruff check .`, `uv run mypy --strict src`, and `uv run pytest` (357 passed).
- Files changed
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/application/contracts.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/unit/test_application_contracts.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/unit/test_application_parsing.py
- /Users/Oliver.Koeth/work/mealplan-cli/AGENT.md
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/prd.json
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/progress.txt
- **Learnings for future iterations:**
- Patterns discovered (e.g., "this codebase uses X for Y")
- Boundary DTO additions should include strict typing plus parsing tests that assert stable error categories/types rather than brittle full-message snapshots.
- Gotchas encountered (e.g., "don't forget to update Z when changing W")
- `make quality` assumes `.venv/bin/uv`; when that path is absent in local runs, execute equivalent gates directly with `uv run ...`.
- Useful context (e.g., "the evaluation panel is in component X")
- Contract units metadata is asserted in tests; when adding new nutrition boundary fields, update `CONTRACT_UNITS_POLICY` and its test expectation together.
---

## [2026-04-08 15:47:06 CEST] - US-002
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-10.txt
- What was implemented
- Added `JsonFoodLogStore` in infrastructure for dedicated file-backed food logging (`food-log.json` in tests), including create and update flows.
- Implemented create semantics with backend-generated UUIDs, update semantics requiring existing UUID, and deterministic unknown-UUID errors (`log.<uuid>: entry not found`).
- Implemented quantity scaling in persistence flow so `kcal`, `carbs`, `fat`, `protein`, and `fiber` are multiplied before save; `quantity` is intentionally not persisted.
- Added robust store I/O handling to auto-create missing storage files and reject invalid JSON root shapes with deterministic config errors.
- Exported the new adapter from `mealplan.infrastructure`.
- Added/ran unit tests for create/update behavior, quantity scaling, file creation, validation guardrails, and config-error paths.
- Ran quality checks successfully using `uv run ruff check .`, `uv run mypy --strict src`, and `uv run pytest` (363 passed).
- Files changed
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/infrastructure/food_log_store.py
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/infrastructure/__init__.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/unit/test_food_log_store.py
- /Users/Oliver.Koeth/work/mealplan-cli/AGENT.md
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/prd.json
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/progress.txt
- **Learnings for future iterations:**
- Patterns discovered (e.g., "this codebase uses X for Y")
- For new JSON persistence adapters, preserve the existing calendar-store ergonomics: lazy store bootstrap, deterministic error-prefix taxonomy, and JSON-object root validation.
- Gotchas encountered (e.g., "don't forget to update Z when changing W")
- `FoodLogUpsertRequest` defaults `quantity` to `1.0`; adapter logic still must apply multiplier explicitly and never persist `quantity`.
- Useful context (e.g., "the evaluation panel is in component X")
- `FoodLogEntry` is the canonical persisted/returned shape for log records, so store writes should serialize from that model (`model_dump(mode=\"json\")`) to keep boundaries aligned.
---

## [2026-04-08 15:50:08 CEST] - US-003
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-2.txt
- What was implemented
- Added `JsonFoodLogStore.search(request=FoodLogSearchRequest)` with optional-AND filtering over persisted entries (`date` exact match, `meal` exact match, `name` case-insensitive substring match).
- Implemented canonical search-result ordering as newest-first by date, returning canonical `FoodLogEntry` objects.
- Added store unit coverage for optional-AND behavior, single-filter cases, case-insensitive substring matching, newest-first ordering, and canonical entry-shape returns.
- Ran quality checks successfully using `uv run ruff check .`, `uv run mypy --strict src`, and `uv run pytest` (369 passed).
- Files changed
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/infrastructure/food_log_store.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/unit/test_food_log_store.py
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/prd.json
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/progress.txt
- /Users/Oliver.Koeth/work/mealplan-cli/AGENT.md
- **Learnings for future iterations:**
- Patterns discovered (e.g., "this codebase uses X for Y")
- Food-log search semantics are centralized in `JsonFoodLogStore.search`; keep CLI/API adapters thin and reuse this method to preserve consistent optional-AND filtering and ordering behavior.
- Gotchas encountered (e.g., "don't forget to update Z when changing W")
- `FoodLogSearchRequest.name` matching is intentionally case-insensitive substring, so future tests/clients should not assume exact-name matching.
- Useful context (e.g., "the evaluation panel is in component X")
- Search responses should be emitted as canonical `FoodLogEntry` models to keep boundary shape aligned for upcoming CLI/REST stories.
---

## [2026-04-08 15:55:13 CEST] - US-004
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-3.txt
- What was implemented
- Added a top-level `mealplan log` Typer sub-app with create/update upsert behavior backed by `JsonFoodLogStore`.
- Implemented required field-flag mode (`--date --meal --name --kcal --carbs --fat --protein --fiber`), optional `--quantity` defaulting to contract value `1.0`, and one-shot `--json` payload mode.
- Enforced `--json` exclusivity against individual field flags; payloads with `uuid` now route to update, payloads without `uuid` route to create.
- Added deterministic log-store path resolution via `MEALPLAN_FOOD_LOG_STORE_PATH` override with default `~/.mealplan/food-log.json`.
- Added CLI tests covering create, quantity scaling, JSON mode, update-via-uuid, mixed-input rejection, unknown-uuid domain error mapping, and `mealplan log --help` JSON example text.
- Ran quality checks successfully using `uv run ruff check .`, `uv run mypy --strict src`, and `uv run pytest` (376 passed).
- Files changed
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/cli/main.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/cli/test_log.py
- /Users/Oliver.Koeth/work/mealplan-cli/AGENT.md
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/prd.json
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/progress.txt
- **Learnings for future iterations:**
- Patterns discovered (e.g., "this codebase uses X for Y")
- For new CLI feature stores, follow the calendar pattern: expose a dedicated `MEALPLAN_*_STORE_PATH` env override plus a stable home-directory default path to keep tests isolated and CLI behavior deterministic.
- Gotchas encountered (e.g., "don't forget to update Z when changing W")
- To keep `mealplan log search` extensible for the next story, `log` is now a Typer sub-app callback; future `search` work should be added as a `log_app.command(...)` subcommand rather than a new top-level command.
- Useful context (e.g., "the evaluation panel is in component X")
- `mealplan log --help` now includes an explicit `--json` example in sub-app help text, and CLI coverage lives in `tests/cli/test_log.py`.
---

## [2026-04-08 16:53:30 CEST] - US-006
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-1.txt
- What was implemented
- Added REST food-log write routes in the UI server: `POST /api/v1/log` (create) and `PUT /api/v1/log/{uuid}` (update).
- Wired both handlers to parse `FoodLogUpsertRequest` via `parse_contract(...)`, so date/meal and payload-shape validation stays at the canonical contract boundary.
- Added UUID-path update semantics by injecting `{uuid}` from route into the parsed payload before calling `JsonFoodLogStore.update(...)`.
- Implemented structured unknown-UUID handling for log updates: `log.<uuid>: entry not found` now maps to HTTP `404` with envelope code `log_not_found`.
- Added UI-server tests covering create success, update success, unknown-UUID 404 mapping, and invalid-date 400 validation mapping.
- Ran quality checks successfully using `uv run ruff check .`, `uv run mypy --strict src`, and `uv run pytest` (382 passed).
- Files changed
- /Users/Oliver.Koeth/work/mealplan-cli/src/mealplan/web/ui_server.py
- /Users/Oliver.Koeth/work/mealplan-cli/tests/unit/test_web_ui_server.py
- /Users/Oliver.Koeth/work/mealplan-cli/AGENT.md
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/prd.json
- /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/progress.txt
- **Learnings for future iterations:**
- Patterns discovered (e.g., "this codebase uses X for Y")
- For write routes that include an entity identifier in the URL, keep the path identifier authoritative and inject it into boundary contracts before persistence to avoid body/path drift.
- Gotchas encountered (e.g., "don't forget to update Z when changing W")
- `parse_contract(...)` date validation details currently surface `Value error, expected YYYYMMDD`; endpoint tests should assert stable field + canonical message wording.
- Useful context (e.g., "the evaluation panel is in component X")
- UI server API tests live in `tests/unit/test_web_ui_server.py` and use local HTTP helpers (`_post_json`, `_put_json_expect_http_error`) against a real in-process server thread for route-level verification.
---

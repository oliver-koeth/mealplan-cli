# Ralph Progress Log
Started: Wed Mar  4 15:29:43 CET 2026
## Codebase Patterns
- Response payloads may include at most one optional `training` meal; keep core canonical-order and macro reconciliation invariants scoped to the six canonical meals only.
- Insert `training` deterministically before `training_before_meal`; validate response meal order by filtering out `training` then matching canonical meal order.
- Typer option annotations cannot be unions with multiple non-`None` members; accept `str | None` in CLI and enforce richer value semantics in contract + semantic validation.
---
## [2026-03-04 15:35:44 CET] - US-001
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-8.txt
- Implemented conditional training-meal emission so output now appends exactly one carbs-only `training` meal when `training_carbs_g > 0`, and emits no training meal when `training_carbs_g == 0`.
- Extended response-contract validation to allow at most one trailing `training` meal while preserving canonical ordering for the six base meals.
- Updated CLI renderers, unit/integration tests, and application golden snapshots for training-meal scenarios.
- Files changed:
  - AGENT.md
  - src/mealplan/application/contracts.py
  - src/mealplan/application/orchestration.py
  - src/mealplan/cli/main.py
  - src/mealplan/domain/services.py
  - tests/cli/test_calculate.py
  - tests/golden/application/non_periodized_with_training.golden.json
  - tests/golden/application/periodized_with_training.golden.json
  - tests/unit/test_application_contracts.py
  - tests/unit/test_application_orchestration.py
  - tests/unit/test_domain_services.py
  - scripts/ralph/prd.json
  - scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: training meal support is treated as response-layer augmentation; canonical six-meal domain invariants remain unchanged and should exclude `training` rows.
  - Gotchas encountered: strict tests and golden fixtures assume exact meal-list length/order, so introducing optional output rows requires coordinated updates across domain, orchestration, and golden snapshots.
  - Useful context: text/table CLI rendering should iterate emitted `payload["meals"]` directly so optional non-canonical rows are displayed consistently with JSON output.
---
## [2026-03-04 15:41:21 CET] - US-002
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-1.txt
- Implemented deterministic training-meal insertion before the selected `training_before_meal` target instead of always appending at the end.
- Threaded `training_before_meal` through application assembly orchestration into domain payload construction.
- Updated response contract validation to allow one optional `training` row at any position while enforcing canonical order of non-training meals.
- Added/updated regression coverage for insertion-before-target behavior across all supported meal targets, orchestration integration expectations, CLI order assertion, and application golden fixtures.
- Files changed:
  - AGENT.md
  - src/mealplan/application/contracts.py
  - src/mealplan/application/orchestration.py
  - src/mealplan/domain/services.py
  - tests/cli/test_calculate.py
  - tests/golden/application/non_periodized_with_training.golden.json
  - tests/golden/application/periodized_with_training.golden.json
  - tests/unit/test_application_contracts.py
  - tests/unit/test_application_orchestration.py
  - tests/unit/test_domain_services.py
  - scripts/ralph/prd.json
  - scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: training-meal ordering is enforced at assembly time by index insertion against canonical meals and should remain deterministic for every `MealName`.
  - Gotchas encountered: integration and golden tests often encode explicit meal order lists; when insertion semantics change, update both expected ordering logic and fixture payloads together.
- Useful context: response DTO validation should ignore the optional `training` row when checking canonical order to keep domain invariants and response flexibility compatible.
---
## [2026-03-04 15:45:17 CET] - US-003
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-2.txt
- Implemented semantic runtime rejection for `--training-before=training` while keeping the value parseable through CLI and request contract parsing.
- Expanded request contract typing so `training_before_meal` accepts `"training"` syntactically, then enforced deterministic semantic validation error `training_session.training_before_meal: must not be 'training'`.
- Added regression coverage in semantic validation matrix and CLI integration to ensure parseable input is rejected at runtime with validation exit code.
- Files changed:
  - AGENT.md
  - src/mealplan/application/contracts.py
  - src/mealplan/application/orchestration.py
  - src/mealplan/application/validation.py
  - src/mealplan/cli/main.py
  - tests/cli/test_calculate.py
  - tests/unit/test_validation_matrices.py
  - scripts/ralph/prd.json
  - scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Patterns discovered: to make a CLI value syntactically parseable but semantically invalid, accept broad CLI typing (`str`) and push strict interpretation into contract + semantic validation.
  - Gotchas encountered: Typer asserts on option unions containing multiple non-`None` types, which can break all commands at startup if used in function annotations.
  - Useful context: keep semantic validation errors field-prefixed (`training_session.training_before_meal: ...`) so exit-code mapping and existing CLI/error tests stay stable.
---

# Ralph Progress Log
## Codebase Patterns
- For new domain orchestration APIs in `src/mealplan/domain/services.py`, add a stable `inspect.signature(...)` regression test in `tests/unit/test_domain_services.py` to prevent interface drift.
- For meal-assembly domain outputs, construct `MealAllocation` rows and validate with `validate_meal_allocation_invariants` before converting to response dictionaries.
- For Phase 7 meal-split precision rules, assert per-meal macros are rounded with `round(..., 2)` only when serializing meal payload fields, while top-level macro fields remain canonical inputs.
- For Phase 7 assembler carb inputs, validate allocation map keys against exact `CANONICAL_MEAL_ORDER` coverage (no missing/extra keys) and raise `DomainRuleError` prefixed `meal_assembly.carb_allocation`.
- For Phase 7 reconciliation, adjust rounding residuals only on the canonical last meal (`evening-snack`) in fixed macro order (`carbs_g`, `protein_g`, `fat_g`), and fail with `meal_assembly.reconciliation` if totals still drift.
- For Phase 7 response-shape changes, validate domain payload compatibility by parsing assembler output with `MealPlanResponse.model_validate(...)` in domain service tests.
- For Phase 7 reconciliation failure-path coverage, use sub-cent macro targets (more than two decimals) so drift remains unreconcilable after cent-based adjustment, then assert the stable `meal_assembly.reconciliation` prefix.
- For phase-contract documentation updates, keep topic ownership aligned: architecture boundaries in `docs/ARCHITECTURE.md`, functional rules in `docs/REQUIREMENTS.md`, and API/invariant specifics in `docs/MODEL.md`.

Started: Tue Mar  3 14:50:04 CET 2026
---
## [2026-03-03 14:54:59 CET] - [US-002]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-1.txt
- Implemented canonical meal assembly through `MealAllocation` construction plus `validate_meal_allocation_invariants` in the domain assembler, ensuring six-meal/order/uniqueness invariants are enforced before payload serialization.
- Strengthened domain service tests to explicitly assert meal order, uniqueness, and canonical coverage in assembler output.
- Files changed
  - `src/mealplan/domain/services.py`
  - `tests/unit/test_domain_services.py`
  - `AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Meal output shape invariants are intended to be guarded via `MealAllocation` + `validate_meal_allocation_invariants`, even when final output is a dict payload.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Local test commands should run through `.venv/bin/uv`/`make quality`; plain `pytest` may not be available on PATH.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Canonical meal order assertions in assembler tests should include sequence equality and uniqueness/coverage checks to prevent subtle contract drift.
---
## [2026-03-03 14:51:41 CET] - [US-001]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-9.txt
- Implemented `calculate_meal_split_and_response_payload(...)` in the domain services layer to assemble a deterministic response payload with canonical top-level fields and six ordered meals.
- Exported the new assembler API from `mealplan.domain` package surface.
- Added unit coverage for stable API signature and canonical payload shape/order.
- Files changed
  - `src/mealplan/domain/services.py`
  - `src/mealplan/domain/__init__.py`
  - `tests/unit/test_domain_services.py`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Domain-service interfaces are treated as contracts and validated with explicit signature snapshot tests.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Ruff lint enforces import sorting and may flag comprehensions where `dict(zip(...))` is preferred (`C416`).
  - Useful context (e.g., "the evaluation panel is in component X")
    - Response payload top-level key is `TDEE` (legacy naming retained), while meal order should always follow `CANONICAL_MEAL_ORDER`.
---
## [2026-03-03 14:56:55 CET] - [US-003]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-2.txt
- Added Phase 7 meal-split regression coverage to assert protein/fat are distributed equally across all six canonical meals using exact pre-rounding float arithmetic.
- Added explicit no-early-rounding assertions using fractional per-meal values to lock behavior ahead of later boundary-rounding stories.
- Files changed
  - `tests/unit/test_domain_services.py`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For split-precision stories, use non-divisible macro totals to detect accidental early `round(..., 2)` behavior.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Ruff line-length (`E501`) is strict at 100 chars; long descriptive test names need shortening.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing canonical payload-shape test already verifies equal split for divisible values; fractional cases are required to prove pre-rounding behavior.
---
## [2026-03-03 14:59:04 CET] - [US-004]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-3.txt
- Implemented strict carb allocation map validation in the Phase 7 meal assembler to enforce exact canonical meal-key coverage before meal-row construction.
- Added deterministic domain error handling for missing/extra carb allocation keys with stable prefix `meal_assembly.carb_allocation`.
- Added unit tests for missing-key and extra-key carb allocation failures while preserving carb carry-through behavior assertions.
- Files changed
  - `src/mealplan/domain/services.py`
  - `tests/unit/test_domain_services.py`
  - `AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Phase 7 meal assembly should fail fast on carb allocation key-shape drift before constructing `MealAllocation` rows to avoid nondeterministic `KeyError` behavior.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - New Phase 7 error tests should assert stable error-prefix regex (`^meal_assembly\\.carb_allocation:`) instead of full message text snapshots.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Because typed mappings can still carry runtime-invalid keys, tests use `typing.cast` to inject extra-key scenarios and verify domain guards.
---
## [2026-03-03 15:01:24 CET] - [US-005]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-4.txt
- Implemented two-decimal boundary rounding for per-meal macro fields (`carbs_g`, `protein_g`, `fat_g`) in Phase 7 response payload assembly.
- Updated domain-service tests to verify deterministic two-decimal outputs for fractional scenarios and preserved canonical top-level macro inputs.
- Files changed
  - `src/mealplan/domain/services.py`
  - `tests/unit/test_domain_services.py`
  - `AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Meal macro rounding policy is a serialization-boundary concern in `calculate_meal_split_and_response_payload`, not a split-calculation concern.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Existing pre-rounding split assertions must be adjusted once Phase 7 boundary rounding is introduced; otherwise they fail on expected rounded payload values.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Representative fractional carb fixtures should include thousandths to exercise deterministic two-decimal boundary behavior.
---
## [2026-03-03 15:05:33 CET] - [US-006]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-5.txt
- Implemented deterministic Phase 7 residual reconciliation after meal-boundary rounding, applying per-macro corrections only to `evening-snack` in fixed order `carbs_g`, `protein_g`, `fat_g`.
- Added post-adjustment reconciliation guard that raises `DomainRuleError` with prefix `meal_assembly.reconciliation` when rounded meal totals still mismatch macro targets.
- Updated domain-service tests to assert evening-snack-only residual placement and updated existing rounding/equal-split assertions to match reconciliation behavior.
- Files changed
  - `src/mealplan/domain/services.py`
  - `tests/unit/test_domain_services.py`
  - `AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Phase 7 reconciliation is part of payload assembly boundary logic: first round each meal macro to two decimals, then apply residuals to canonical last meal only.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Existing tests that expect identical per-meal split values must account for evening-snack residual adjustments once reconciliation is enabled.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Strict typing in domain services is easiest to preserve by using internal `TypedDict` rows for meal payload arithmetic while keeping public API return type as `dict[str, object]`.
---
## [2026-03-03 15:08:14 CET] - [US-007]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-6.txt
- Implemented a dedicated response assembly helper in `domain/services.py` to build top-level fields plus `meals[]`, and routed the Phase 7 assembler API through that helper.
- Added tests for helper output shape/field presence and contract compatibility by validating assembled payloads against `MealPlanResponse`.
- Files changed
  - `src/mealplan/domain/services.py`
  - `tests/unit/test_domain_services.py`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - For response-shape stories, keep a focused payload-construction helper and lock compatibility by parsing with `MealPlanResponse.model_validate(...)`.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - `MealPlanResponse` uses strict floats and canonical meal-order validation, so helper tests should feed float literals and canonical `CANONICAL_MEAL_ORDER` rows.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Current payload assembly helper remains domain-local (`_assemble_meal_split_response_payload`) while the public contract stays `calculate_meal_split_and_response_payload(...)`.
---
## [2026-03-03 15:10:34 CET] - [US-008]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-7.txt
- Added reconciliation policy tests for both no-adjustment and drift-failure paths in Phase 7 meal assembly.
- Added a deterministic no-adjustment test that verifies evening-snack remains unchanged when rounded meal totals already match top-level targets.
- Added an unreconcilable sub-cent target test that asserts `DomainRuleError` with stable `meal_assembly.reconciliation` prefix when cent-based residual adjustment cannot match top-level totals.
- Files changed
  - `tests/unit/test_domain_services.py`
  - `AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Reconciliation failure-path tests are easiest to trigger with sub-cent macro targets because cent-rounded residual steps cannot perfectly satisfy those totals.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Assert only the stable error prefix (`^meal_assembly\\.reconciliation:`) for reconciliation failures instead of brittle full-message snapshots.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing drift-correction test already covers the successful residual path; this story needed explicit no-adjustment and unreconcilable-path tests.
---
## [2026-03-03 15:14:37 CET] - [US-009]
Codex output: /Users/Oliver.Koeth/work/mealplan-cli/scripts/ralph/.codex-last-message-iter-8.txt
- Documented Phase 7 domain assembly boundary in architecture docs, including canonical API, two-decimal meal-boundary rounding, and deterministic residual reconciliation policy.
- Added explicit functional requirements for Phase 7 response assembly: canonical six-meal ordering, boundary-only rounding, fixed residual target (`evening-snack`), and fixed macro correction order (`carbs_g`, `protein_g`, `fat_g`).
- Expanded model contracts with a dedicated Phase 7 meal assembly section defining API signature, input/output contract, key-coverage validation, rounding boundary, and reconciliation failure behavior.
- Files changed
  - `docs/ARCHITECTURE.md`
  - `docs/REQUIREMENTS.md`
  - `docs/MODEL.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered (e.g., "this codebase uses X for Y")
    - Phase-level documentation updates should keep architecture boundary ownership in `ARCHITECTURE.md`, functional rule statements in `REQUIREMENTS.md`, and domain API/invariant details in `MODEL.md`.
  - Gotchas encountered (e.g., "don't forget to update Z when changing W")
    - Phase 7 docs need both residual target meal (`evening-snack`) and deterministic macro correction order (`carbs_g`, `protein_g`, `fat_g`) stated explicitly to avoid ambiguity.
  - Useful context (e.g., "the evaluation panel is in component X")
    - Existing docs already contained partial rounding/reconciliation language; this story required tightening them into explicit contract wording.
---
